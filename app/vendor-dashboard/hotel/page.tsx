"use client";

// import { useEffect } from "react";
// import { useRouter } from "next/navigation";

export default function HotelVendorPage() {
  // const router = useRouter();

  // useEffect(() => {
  //   // Redirect to the appropriate hotel dashboard
  //   router.replace("/vendorDashboard");
  // }, [router]);

  return (
    <div className="flex items-center justify-center min-h-screen">
      <div className="text-center">
        <h2 className="text-lg font-semibold">Redirecting to Hotel Dashboard...</h2>
        <p className="text-gray-600 mt-2">Please wait while we load your dashboard.</p>
      </div>
    </div>
  );
}
